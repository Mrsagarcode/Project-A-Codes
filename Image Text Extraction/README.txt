"This application is bundled as a standalone executable in the 'dist' directory. 
To run the program, simply navigate to the 'dist' folder and execute the 
application. Ensure that the required XML files or additional resources are in 
the same directory for the application to work correctly."